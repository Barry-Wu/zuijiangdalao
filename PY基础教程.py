##PY函数大全（Built-in Functions）
`abs()`：绝对值
*像`abs(-3)`这种就是取绝对值，但是像`abs(2+1j)`这样就是取模。
`all()`：全
*也称∀
`any()`：有
*也称∃
`ascii()`
*？？？
`bin()``oct()``hex()`：二进制化、八进制化、十六进制化
*比如输入`bin(123)`得`0b1111011`，输入`oct(123)`得`0o173`，输入`hex(123)`得`0x7b`。
*由此可见，二、八、十六进制数前面分别会带0b、0o、0x，前面啥也不带的必为十进制数。
*在这俩函数这，我才发现很多函数的自变量可以是上述任何一种进制数，甚至True和False都行。
`bool()`：逻辑值
*根据里面的算式情况，输出True或者False。
`bytearray()`
*？？？
`bytes()`
*？？？
`callable()`
*？？？
`chr()`
*？？？
`classmethod()`
*？？？
`compile()`
*？？？
`complex()`
*？？？
`delattr()`
*？？？
`dict()`
*？？？
`dir()`
*？？？
`divmod()`
*？？？
`enumerate()`
*？？？
`eval()`
*？？？
`exec()`
*？？？
`filter()`
*？？？
`float()`
*？？？
`format()`
*？？？
`frozenset()`
*？？？
`getattr()`
*？？？
`globals()`
*？？？
`hasattr()`
*？？？
`hash()`
*？？？
`help()`
*？？？
`id()`
*？？？
`__import__()`
*？？？
`input()`
*？？？
`int()`
*？？？
`isinstance()`
*？？？
`issubclass()`
*？？？
`iter()`
*？？？
`len()`
*？？？
`list()`
*？？？
`locals()`
*？？？
`map()`
*？？？
`max()``min()`：最大值、最小值
*比如输入`min(-1,2,3,5,6)`得`6`，输入min`(-1,2,3,5,6)`得`-1`。
*实数之间都可以比较。憋整那些复数、矩阵啥的！
`memoryview()`
*？？？
`next()`
*？？？
`object()`
*？？？
`open()`
*？？？
`ord()`
*？？？
`pow()`
*？？？
`print()`
*？？？
`property()`
*？？？
`range()`
*？？？
`repr()`
*？？？
`reversed()`
*？？？
`round()`
*？？？
`set()`
*？？？
`setattr()`
*？？？
`slice()`
*？？？
`sorted()`
*？？？
`staticmethod()`
*？？？
`str()`
*？？？
`sum()`
*？？？
`super()`
*？？？
`tuple()`
*？？？
`type()`
*？？？
`vars()`
*？？？
`zip()`
*？？？
